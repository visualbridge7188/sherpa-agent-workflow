# Duo 부고알림 도메인 컨텍스트

## 도메인

이 저장소는 WordPress KBoard 기반 `Duo 부고알림` 게시판 스킨과 이를 플러그인 형태로 배포하는 패키지를 관리한다.

## 용어

- **부고 게시판**: KBoard 게시판 중 `Duo 부고알림` 스킨을 사용하는 게시판.
- **부고 글**: 고인과 장례 정보를 담은 KBoard 게시글.
- **부고 입력값**: 소속, 고인명, 상주명, 별세일, 입관일, 발인일, 장례식장, 장지, 사진.
- **표시 컬럼**: 목록 화면과 최신글 화면에 노출되는 부고 입력값.
- **검색어**: KBoard의 `keyword` GET 파라미터. 부고 게시판에서는 표시 컬럼 전체를 검색한다.
- **최신글 위젯**: KBoard `kboard_latest` 숏코드로 표시되는 부고 요약 목록.
- **지난 부고**: 발인일이 현재 날짜보다 이전인 부고 글.
- **패키지 스킨**: `plugins/duo-obituary-kboard/skins/duo-obituary-kboard` 아래에 복사된 배포용 스킨.
- **원본 스킨**: `skin/duo-obituary-kboard` 아래에서 관리하는 스킨 원본.
- **관리자 가이드**: 처음 사용하는 관리자가 설치, 게시판 생성, 부고 등록, 검색, 최신글, CSV 내보내기를 이해하기 위한 문서.

## 운영 규칙

- 원본 스킨과 패키지 스킨은 배포 전에 동기화되어야 한다.
- `scripts/build-plugin.sh`는 원본 스킨을 패키지 스킨으로 복사하고 ZIP을 만든다.
- 배포 ZIP에는 `README.md`와 `ADMIN_GUIDE.md`가 포함되어야 한다.
- 부고 검색은 KBoard 기본 `keyword` 흐름을 유지하되, 표시 컬럼이 저장된 option 테이블 검색을 포함해야 한다.
- 최신글 위젯은 지난 부고를 제외한다.
- 최신글 위젯은 표시 항목이 5개 이하이거나 검색 중이면 롤링하지 않는다.

